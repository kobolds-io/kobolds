const std = @import("std");

/// The number of iterations to be executed for benchmark tests
// pub const benchmark_max_iterations = std.math.maxInt(u16);
pub const benchmark_max_iterations = 1;

/// The number of items added to the data_list
pub const benchmark_max_queue_data_list = 1_000;
